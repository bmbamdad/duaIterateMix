Just Simply copy and paste all of the files here to your computer and use the below command line as an example 

python duaIterateMix.py -n Falls.net.XML -m=true -j=true -z=true -t trips.trips.xml -r trips.trips.CAV.xml sumo--duration-log.statistics sumo--statistic-output stats.xml --logit --max-alternatives-CAV 5 --PSwap 50  --marginal-cost --marginal-cost.exp 1 --mix
